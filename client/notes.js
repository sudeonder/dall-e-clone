// in state variable declerations : const ensures the variable name always points to the same reference, but it doesn't make the value immutable.

// object-contain : If the image has an aspect ratio that does not match the square aspect ratio of the container, you will see the entire image scaled down with empty space around it (either horizontally or vertically), ensuring the image is not distorted.
